#include <stdio.h>
// Build = compile + link
//  Compiler translates source code (.c file name extension) into object code (.o file name extension)
//  Linker combines object code with C libraries to make a program
int main() 
{
    int a = 0;
    puts("test");
    printf("Is this any different?");
    return 0;
}